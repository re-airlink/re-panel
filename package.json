{
  "name": "panel",
  "version": "1.0.0",
  "description": "Airlink is a simple to use Game server managment panel",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/views/**/* dist/ && copyfiles -u 1 src/public/**/* dist/ && tailwindcss -i src/public/styles.css -o dist/public/styles.css",
    "start": "node dist/app.js",
    "clean": "ts-node -e \"import { removeSync, readdirSync } from 'fs-extra'; readdirSync('dist').forEach(dir => { if (dir !== 'storage') removeSync(`dist/${dir}`); });\"",
    "start:dev": "nodemon"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.8.6",
    "copyfiles": "^2.4.1",
    "fs-extra": "^11.2.0",
    "nodemon": "^3.1.7",
    "tailwindcss": "^3.4.14",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "express": "^4.21.1"
  }
}
